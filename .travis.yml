language: ruby
rvm:
  - 2.1.1
env:
  - DB=postgresql SECRET_TOKEN=Shhhhhhhhhhhhhhhhhhhhhhhhhhhhh
script:
  - createuser --createdb --no-superuser --no-createrole stevegrossi
  - RAILS_ENV=test bundle exec rake db:setup --trace
  - bundle exec rake db:test:prepare
  - bundle exec rake
