<h1>I write things.</h1>
<ol>
<% @writings.each do |writing| %>
  <li>
    <span class="written"><%= writing.created_at.strftime('%B %e, %Y') %></span>
    <article>
      <%= link_to writing.title, writing %>
      <%= writing.summary %>
    </article>
  </li>
<% end %>
</ol>